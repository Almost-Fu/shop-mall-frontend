# 📁 ShopMall 项目目录说明

> 本文档说明项目中**每个文件夹 / 文件放什么、干什么用**，方便你快速定位代码、看懂项目结构。

---

## 一、根目录

| 文件 / 文件夹 | 用途 |
|---|---|
| `index.html` | 唯一 HTML 入口，包含 `<div id="app">` 挂载点和 `<script>` 引入 |
| `package.json` | 项目配置：依赖列表、启动脚本（dev / build / preview） |
| `vite.config.ts` | Vite 构建配置：插件、`@` 路径别名、开发服务器端口 |
| `tsconfig.json` | TS 总配置，用 `references` 引用下面两个子配置 |
| `tsconfig.app.json` | **src 目录**的 TS 配置（浏览器端代码） |
| `tsconfig.node.json` | **vite.config.ts** 的 TS 配置（Node 端） |
| `.env.development` | 开发环境变量（后端接口地址） |
| `.env.production` | 生产环境变量（真实后端接口地址） |
| `.gitignore` | Git 忽略规则（node_modules、dist、.env 等） |
| `README.md` | 项目说明文档（技术栈、启动方式、功能清单） |

---

## 二、src 目录（核心源码）

```
src/
├── api/        接口请求层
├── types/      全局类型定义
├── utils/      工具函数
├── stores/     Pinia 状态管理
├── router/     路由配置
├── layouts/    布局组件
├── components/ 公共组件
├── views/      页面组件
├── main.ts     入口文件
├── App.vue     根组件
├── style.css   全局样式
└── env.d.ts    环境变量类型声明
```

### 1. `src/api/` —— 接口请求层（前后端分离的核心）

**放什么**：所有和后端交互的接口函数，按业务模块拆分文件。

| 文件 | 存放内容 |
|---|---|
| `auth.ts` | 登录接口（`login`） |
| `goods.ts` | 商品接口（列表、详情、分类） |
| `order.ts` | 订单接口（列表、创建） |
| `user.ts` | 用户接口（用户列表） |

> ⚠️ **规则**：页面组件**只能调用这里**的函数，禁止在组件里直接写 axios / 接口地址。
> 对接真实后端时，只需改这个目录（和环境变量），业务页面零改动。

### 2. `src/types/` —— 全局类型定义

**放什么**：接口返回数据、业务实体的 TypeScript 类型。

| 文件 | 存放内容 |
|---|---|
| `index.ts` | Product、Category、Order、UserInfo、PageResult 等所有类型 |

### 4. `src/utils/` —— 工具函数

| 文件 | 存放内容 |
|---|---|
| `request.ts` | axios 统一封装：实例创建、请求拦截（加 token）、响应拦截（统一解包） |

### 5. `src/stores/` —— Pinia 状态管理

**放什么**：多个组件共享的全局状态。

| 文件 | 存放内容 |
|---|---|
| `user.ts` | 登录状态：token、用户信息、角色，登录/退出方法 |
| `cart.ts` | 购物车：商品列表、增删改、勾选、总价计算（本地持久化） |

### 6. `src/router/` —— 路由配置

| 文件 | 存放内容 |
|---|---|
| `index.ts` | 路由表（前台 + 后台）+ **角色路由守卫**（拦截非 admin 访问后台） |

### 7. `src/layouts/` —— 布局组件

| 文件 | 存放内容 |
|---|---|
| `ShopLayout.vue` | 商城前台布局：顶部导航、搜索、购物车图标、用户菜单 |
| `AdminLayout.vue` | 后台布局：侧边菜单、顶栏、内容区 |

### 8. `src/components/` —— 公共组件

| 文件 | 存放内容 |
|---|---|
| `ProductCard.vue` | 商品卡片（首页、列表页复用） |

### 9. `src/views/` —— 页面组件

**放什么**：真正的页面，按前台 / 后台分文件夹。

| 文件夹 | 存放内容 |
|---|---|
| `Login.vue` | 登录页（角色分流：admin 跳后台，user 跳首页） |
| `shop/` | **商城前台**：Home 首页、GoodsList 商品列表、GoodsDetail 商品详情、Cart 购物车 |
| `admin/` | **后台管理**：Dashboard 数据看板、GoodsManage 商品管理、OrdersManage 订单管理、UsersManage 用户管理 |

### 10. 其他根文件

| 文件 | 用途 |
|---|---|
| `main.ts` | 应用入口：创建 Vue 应用、注册 Pinia / Router / ElementPlus、挂载 |
| `App.vue` | 根组件（路由出口） |
| `style.css` | 全局样式：CSS 变量、主题色、封面渐变色组、通用类 |
| `env.d.ts` | 环境变量（`import.meta.env`）的 TS 类型声明 |

---

## 三、数据流向（一次请求怎么走）

```
页面组件 (views/)
   │  调用
   ▼
api 接口函数 (src/api/)
   │  调用统一 request
   ▼
utils/request.ts (axios 实例)
   │  请求后端接口
   ▼
FastAPI 后端 (shop-backend，SQLAlchemy + MySQL)
```

---

## 四、对接后端的说明

前端通过 `src/api` 统一请求层对接 FastAPI 后端（配套 shop-backend 项目）：

1. `.env.development` 里 `VITE_API_BASE_URL` 指向后端地址（默认 http://localhost:8000）
2. 页面组件只依赖 `src/api` 暴露的函数，只要接口函数名和返回值结构不变，后端换成谁都行。
